# Visual Guide: Job Description File Upload Feature

## UI Component Overview

### Before (Original)
```
┌─────────────────────────────────────────┐
│ Job Description                         │
├─────────────────────────────────────────┤
│                                         │
│  [Textarea with placeholder text]      │
│  Paste the complete job description    │
│  here. Include requirements...          │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │                                  │  │
│  │                                  │  │
│  │                                  │  │
│  │                                  │  │
│  └──────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

### After (New with Tabs)
```
┌─────────────────────────────────────────────────────────────┐
│ Job Description                                             │
├─────────────────────────────────────────────────────────────┤
│ [Paste Text]  [Upload File]  ← Tab Interface               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📄 Paste Text Tab (Default)                                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ [Textarea - Paste description here]                 │  │
│  │                                                      │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  📁 Upload File Tab (New)                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ ⬇️  Click to upload or drag and drop                │  │
│  │ PDF or DOCX files (max 10MB)                        │  │
│  │                                                      │  │
│  │ ✅ File Selected                                     │  │
│  │ job_description.pdf (245 KB)                        │  │
│  │ [Change file]                                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## User Interaction Flow

### Scenario 1: Using Text Method
```
User Opens Form
    ↓
Sees "Paste Text" tab (active by default)
    ↓
Pastes job description in textarea
    ↓
Fills other form fields (resume, interview type, etc.)
    ↓
Clicks "Generate Interview Questions"
    ↓
Form submitted to API
    ↓
Questions generated with pasted text
```

### Scenario 2: Using File Method
```
User Opens Form
    ↓
Clicks "Upload File" tab
    ↓
Textarea cleared automatically
    ↓
User uploads PDF or DOCX file
    ├─ Option A: Click upload area to browse
    └─ Option B: Drag file over area
    ↓
File validated:
├─ Check format ✓ (PDF/DOCX)
├─ Check size ✓ (< 10MB)
└─ Check content ✓ (has extractable text)
    ↓
Filename appears: "sales_job_description.docx (89 KB)"
    ↓
Fills other form fields (resume, interview type, etc.)
    ↓
Clicks "Generate Interview Questions"
    ↓
Form submitted to API with file
    ↓
Backend extracts text from file
    ↓
Questions generated with extracted text
```

### Scenario 3: Switching Tabs
```
User in "Paste Text" tab with text entered
    ↓
User clicks "Upload File" tab
    ↓
Pasted text automatically cleared
    ↓
User can now upload a file
    ↓
User clicks back to "Paste Text" tab
    ↓
File selection cleared
    ↓
User can paste text again (fresh start)
```

## File Processing Pipeline

### Text Method (Original - Still Works)
```
┌─────────────────────────────────────┐
│   User pastes JD text in textarea   │
└────────────────┬────────────────────┘
                 │
                 ↓
        ┌────────────────────┐
        │ Form Validation    │
        │ • Text length > 50 │
        └────────┬───────────┘
                 │
                 ↓
        ┌────────────────────────┐
        │ Generate Questions     │
        │ (Resume + Text JD)     │
        └────────────────────────┘
```

### File Method (New)
```
┌──────────────────────────────────┐
│  User uploads PDF or DOCX file   │
└────────────────┬─────────────────┘
                 │
                 ↓
        ┌────────────────────────┐
        │ File Validation        │
        │ • Format check (.pdf   │
        │   or .docx)            │
        │ • Size check (< 10MB)  │
        └────────────┬───────────┘
                     │
                     ↓
    ┌────────────────────────────┐
    │ Text Extraction            │
    │ ├─ PDF: Use pdfplumber    │
    │ └─ DOCX: Use python-docx  │
    └────────────┬───────────────┘
                 │
                 ↓
    ┌────────────────────────────┐
    │ Form Validation            │
    │ • Extracted text present   │
    └────────────┬───────────────┘
                 │
                 ↓
    ┌────────────────────────────┐
    │ Generate Questions         │
    │ (Resume + Extracted JD)    │
    └────────────────────────────┘
```

## Component Hierarchy

```
Job Description Section
│
├── Tab Container
│   ├── "Paste Text" Tab Button
│   │   └── Click Handler: Show text area, hide file area
│   │
│   └── "Upload File" Tab Button
│       └── Click Handler: Show file area, hide text area
│
├── Text Input Area (Initially Visible)
│   └── textarea#job_description
│       └── Validation: min 50 chars OR file present
│
└── File Upload Area (Initially Hidden)
    ├── input#job_description_file (type=file)
    │   └── Accept: .pdf, .docx
    │
    ├── Upload Zone (Drag & Drop)
    │   ├── Drag Enter: Add hover effect
    │   ├── Drag Leave: Remove hover effect
    │   ├── Drop: Handle file upload
    │   └── Click: Open file browser
    │
    ├── Upload Content (Initially Visible)
    │   └── "Click to upload or drag and drop"
    │
    └── File Selected (Initially Hidden)
        ├── Filename display
        ├── File size display
        └── "Change file" button
```

## State Machine (Tab/Input States)

```
                    Page Load
                       │
                       ↓
          ┌─────────────────────────┐
          │  TEXT TAB ACTIVE        │
          │  • Textarea visible     │
          │  • File area hidden     │
          └────────┬────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
   User clicks          User clicks
  "Upload File"         "Paste Text"
        │                     │
        ↓                     ↓
┌──────────────────┐  ┌──────────────────┐
│ FILE TAB ACTIVE  │  │ TEXT TAB ACTIVE  │
│ • File area      │  │ • Textarea       │
│   visible        │  │   visible        │
│ • Textarea       │  │ • File area      │
│   hidden         │  │   hidden         │
│ • Text cleared   │  │ • File cleared   │
└────────┬─────────┘  └────────┬─────────┘
         │                     │
         └─────────┬───────────┘
                   │
            (Can switch back and forth)
```

## API Request Structure

### Request with Text JD
```http
POST /api/v1/generate-questions
Content-Type: multipart/form-data

resume: [PDF/DOCX/TXT file]
job_description: "Full text of job description..."
job_description_file: (not included)
round_type: "technical"
difficulty: "intermediate"
num_questions: 10
api_key: "gemini_api_key_here"
```

### Request with File JD
```http
POST /api/v1/generate-questions
Content-Type: multipart/form-data

resume: [PDF/DOCX/TXT file]
job_description: (empty or not included)
job_description_file: [PDF or DOCX file]
round_type: "technical"
difficulty: "intermediate"
num_questions: 10
api_key: "gemini_api_key_here"
```

## Response Flow (Same for Both Methods)

```
API Receives Request
        │
        ├─→ Parse Resume ✓
        ├─→ Extract/Validate JD ✓
        ├─→ Generate Questions ✓
        │
        ↓
┌───────────────────────────────┐
│ QuestionGenerationResponse    │
│ {                             │
│   "questions": [...],         │
│   "total_questions": 10,      │
│   "round_type": "technical",  │
│   "difficulty": "intermediate"│
│ }                             │
└───────────────────────────────┘
        │
        ↓
     Display Results
```

## Visual States During Interaction

### State 1: Initial Load
```
┌─────────────────────────────────┐
│ [Paste Text] [Upload File]      │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ [Textarea - paste here...]  │ │
│ │                             │ │
│ │ 0 characters (minimum 50)   │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### State 2: File Upload Tab Active
```
┌─────────────────────────────────┐
│ [Paste Text] [Upload File] ●    │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │  ⬇️  Drag files here         │ │
│ │  PDF or DOCX (max 10MB)     │ │
│ │  [Click to browse]          │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### State 3: File Selected
```
┌─────────────────────────────────┐
│ [Paste Text] [Upload File] ●    │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ ✅ jd_2025.pdf (145 KB)     │ │
│ │ [Change file]               │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### State 4: Hover Over Upload Area
```
┌─────────────────────────────────┐
│ [Paste Text] [Upload File] ●    │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │╔═══════════════════════════╗│ │  ← Hover effect
│ │║  ⬇️  Drop file here!      ║│ │
│ │║  PDF or DOCX (max 10MB)   ║│ │
│ │╚═══════════════════════════╝│ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

## Success Notifications

```
┌──────────────────────────────────────────┐
│ ✓ File uploaded successfully             │ ← Toast
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ ✓ Questions generated successfully!      │ ← Toast
└──────────────────────────────────────────┘
```

## Error Notifications

```
┌──────────────────────────────────────────┐
│ ✗ Please select a PDF or DOCX file       │ ← Toast
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ ✗ File size must be less than 10MB       │ ← Toast
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ ✗ Failed to extract text from file       │ ← Toast
└──────────────────────────────────────────┘
```

---

This visual guide shows how the UI has evolved and how users interact with the new file upload feature!
